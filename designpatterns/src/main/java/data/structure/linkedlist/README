1, 获取链表长度
    直接遍历获取长度

2, 获取两边倒数第k个数据
    1) 获取链表长度， 倒数第k个， 即正数第(size - k) 个
    2） 可以用栈， 全部入栈后， 出栈K个

3, 单链表反转
    1) 辅助头节点， 每次取出原链表一个元素 添加到新链表的的第一个位置
    2） 使用栈，做入栈出栈操作

4, 逆序打印链表
    1） 链表反转 然后打印， 会破坏链表结构
    2） 利用栈数据结构    入栈， 出栈打印即为逆序打印   （栈： 先入后出）
